<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local notifications (event reminders) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <!-- Biometric lock screen -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Calling invitees directly from the app -->
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <!-- Reading/writing exported Excel/PDF files -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32"/>

    <application
        android:label="دعواتي"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- NOTE: flutter_local_notifications bundles its own boot-rescheduling
             receiver declaration inside its own AAR manifest (merged
             automatically by Gradle), so it is intentionally NOT re-declared
             here — hand-declaring the plugin's internal receiver class name
             risks drifting from whatever version `pub get` resolves. -->
    </application>

    <!-- Needed since Android 11 to query whether a WhatsApp/dialer/browser
         activity is available to handle tel:/https:/wa.me links via url_launcher -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
